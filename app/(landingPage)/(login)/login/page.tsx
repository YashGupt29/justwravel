import { LoginCard } from "../_components/loginCard";

const LoginPage = () => {
  return (
    <div className="h-[870px] bg-gradient-to-b from-blue-500 to-sky-100 flex items-center justify-center">
      <LoginCard />
    </div>
  );
};
export default LoginPage;
